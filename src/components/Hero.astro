<section class="hero">
    <div class="hero-bg"></div>
    <div class="container content">
        <h1>
            Un sistema con IA que te trae clientes <span class="highlight"
                >cualificados</span
            > cada mes
        </h1>
        <p class="subtitle">
            Automatizamos la captación, el seguimiento y la agenda de reuniones
            para negocios que venden servicios de alto valor.
        </p>
        <a href="#demo" class="btn"> Agenda una demo de 15 min &rarr; </a>
    </div>
</section>

<style>
    .hero {
        position: relative;
        padding-top: 180px;
        padding-bottom: 120px;
        text-align: center;
        overflow: hidden;
    }

    .hero-bg {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        background-image:
            linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
            linear-gradient(
                90deg,
                rgba(255, 255, 255, 0.03) 1px,
                transparent 1px
            );
        background-size: 40px 40px;
        background: radial-gradient(
            circle at 50% 30%,
            rgba(37, 99, 235, 0.15) 0%,
            rgba(7, 9, 16, 0) 60%
        );
    }

    .content {
        max-width: 800px;
        margin: 0 auto;
    }

    h1 {
        font-size: 3.5rem;
        margin-bottom: 24px;
        letter-spacing: -1px;
    }

    .highlight {
        color: var(--accent-light);
    }

    .subtitle {
        font-size: 1.125rem;
        color: var(--text-muted);
        margin-bottom: 40px;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
    }

    @media (max-width: 768px) {
        h1 {
            font-size: 2.5rem;
        }
        .hero {
            padding-top: 140px;
        }
    }
</style>
